swagger: '2.0'
info:
  description: 'A sample object containing standard properties'
  title: 'Sample: flat object'
  version: 1.2.2

definitions:

  FlatObjectWithEnum:
    type: object
    required:
      - id
      - name
    properties:
      id:
        type: string
        enum: ["a", "b", "c"]
